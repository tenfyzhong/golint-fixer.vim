Given go (remove underscore):
  package te_st

Execute (GoLintFix):
  call setqflist([{'lnum': 1, 'bufnr': 1, 'col': 1, 'valid': 1, 'vcol': 0, 
    \'nr': -1, 'type': 'W', 'pattern': '', 
    \'text': 'don''t use an underscore in package name'}])
  GoLintFix

Expect go (after fix):
  package test
